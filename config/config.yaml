---
defaults:
  - _self_
  - features_config.yaml
  - model_config.yaml
  - training_config.yaml

# Hydra specific
# TODO: Add help message
hydra:
  help:
    template:
      'TODO: Add help message'
  run:
    dir:
      outputs/${run_mode}_output/${now:%Y-%m-%d_%H-%M-%S}

# Logging settings
# TODO: actually implement this
log_level: DEBUG
tf_log_level: 1

# Run mode
run_mode: train
use_gpu: True
step_size: 5 GB  # sets memory used by dataloaders

# glob-able paths to input files
TauFiles: "/hepgpu5-data3/benwilson/split_NTuples/*Gammatautau*/*.root"
FakeFiles: "/hepgpu5-data3/benwilson/split_NTuples/*JZ*/*.root"
